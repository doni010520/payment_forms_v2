-- =====================================================================
-- Migration 001 — baseline (marca que schema.sql foi aplicado)
-- O schema.sql é carregado por initSchema() na 1ª subida. Esta migração
-- apenas marca que a baseline foi aplicada — não tem mudanças adicionais.
-- =====================================================================
SELECT 1;  -- no-op (baseline já criada por schema.sql)
